sudo: required
language: generic

# tell travis we need a copy of docker running
services:
    - docker
# build our image using Dockerfile.dev
before_install:
  - docker build -t react-app -f Dockerfile.dev .

script:
  - docker run -e CI=true alemoca/react-app npm run test -- --coverage
